apiVersion: apps/v1
kind: Deployment
metadata:
  name: cart-service-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cart-service-redis
  template:
    metadata:
      labels:
        app: cart-service-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: cart-service-redis
spec:
  selector:
    app: cart-service-redis
  ports:
    - port: 6379
      targetPort: 6379
