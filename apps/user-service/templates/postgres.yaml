apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-service-postgres
  template:
    metadata:
      labels:
        app: user-service-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: users
            - name: POSTGRES_USER
              value: users
            - name: POSTGRES_PASSWORD
              value: users
---
apiVersion: v1
kind: Service
metadata:
  name: user-service-postgres
spec:
  selector:
    app: user-service-postgres
  ports:
    - port: 5432
      targetPort: 5432
