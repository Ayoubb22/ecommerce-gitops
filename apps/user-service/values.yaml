replicaCount: 1

image:
  repository: user-service
  tag: "local"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi

probes:
  liveness:
    path: /actuator/health/liveness
    initialDelaySeconds: 90
  readiness:
    path: /actuator/health/readiness
    initialDelaySeconds: 60

autoscaling:
  enabled: false
  minReplicas: 2
  maxReplicas: 5
  targetCPUUtilizationPercentage: 70

env:
  - name: DB_HOST
    value: user-service-postgres
  - name: DB_PORT
    value: "5432"
  - name: DB_NAME
    value: users
  - name: DB_USER
    value: users
  - name: DB_PASSWORD
    value: users
  - name: JWT_SECRET
    value: dev-secret-please-change-me-1234567890
